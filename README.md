---

# Sitemap SEO Checker Pro v2.0

**Sitemap SEO Checker Pro** — это мощный и легкий PHP-инструмент для комплексного аудита страниц сайта на основе данных из XML-карты (sitemap.xml). Скрипт анализирует технические параметры и SEO-показатели каждой страницы, предоставляя отчет в реальном времени через современный адаптивный интерфейс.

## 🚀 Основные возможности

* **Парсинг Sitemap:** Поддержка стандартных XML-файлов, индексов карт сайтов (Sitemap Index) и сжатых архивов `.gz`.
* **Технический аудит:**
* Проверка HTTP-статусов (200, 404, 500 и т.д.).
* Замер времени ответа сервера (мониторинг медленных страниц).
* Проверка заголовков `X-Robots-Tag`.


* **SEO Анализ:**
* Анализ Meta-тегов (Title, Description, Robots).
* Проверка тега `link rel="canonical"`.
* Подсчет заголовков `H1` (выявление отсутствующих или дублирующихся).
* Автоматическое определение языка контента (RU/EN/Mixed).


* **Анализ ссылок:** Подсчет общего количества ссылок на странице и выявление тегов с атрибутом `rel="nofollow"`.
* **Интеллектуальная валидация:** Система подсвечивает проблемы (слишком короткие/длинные заголовки, отсутствие описания, малый объем контента).
* **Интерфейс и экспорт:**
* Адаптивный дизайн (удобно работать с планшетов и смартфонов).
* Фильтрация результатов (по ошибкам, индексации, языку).
* Экспорт данных в формат **CSV** для дальнейшей работы в Excel или Google Таблицах.



---

## 🛠 Технические требования

* **PHP:** 7.4 или выше.
* **Расширения PHP:** * `curl` (для запросов к страницам).
* `mbstring` (для корректной работы с UTF-8).
* `libxml` & `dom` (для парсинга HTML).
* `zlib` (для работы с gzipped sitemaps).


* **Доступ в интернет:** Скрипт должен иметь возможность отправлять исходящие запросы к анализируемым сайтам.

---

## 📦 Установка и запуск

1. Скачайте файл `sitemap_checker.php`.
2. Загрузите его на ваш веб-сервер (Apache/Nginx) в любую директорию.
3. Откройте файл в браузере: `https://ваш-сайт.com/path/sitemap_checker.php`.
4. Введите URL карты сайта в поле ввода и нажмите **"Проверить"**.

---

## ⚙️ Настройка (Конфигурация)

Вы можете изменить параметры работы скрипта в массиве `$config` в начале файла:

```php
$config = [
    'user_agent' => 'Mozilla/5.0 (compatible; YandexWebmaster/2.0; +http://yandex.com/bots)', // Имя бота
    'timeout' => 15,            // Тайм-аут ожидания ответа (сек)
    'max_urls' => 200,          // Лимит URL для одной проверки
    'slow_page_threshold' => 3, // Порог в секундах, после которого страница считается медленной
    'min_title_length' => 10,   // Минимальная длина Title
    'max_title_length' => 75,   // Максимальная длина Title
];

```

---

## 📊 Какие проблемы выявляет скрипт?

Скрипт автоматически помечает следующие SEO-ошибки:

* **Missing Title / Description:** Отсутствие важных мета-тегов.
* **Title/Desc short/long:** Несоответствие длины тегов рекомендациям поисковых систем.
* **No H1 / Multiple H1:** Проблемы со структурой заголовков.
* **Canonical mismatch:** Если канонический URL не совпадает с текущим.
* **Low content:** Текстовое содержимое страницы менее 200 символов.
* **Slow:** Время загрузки страницы превышает заданный лимит.
* **Noindex/Nofollow:** Предупреждения о запрете индексации страницы или ссылок на ней.

---

## 🛡 Безопасность

* Скрипт использует `CURLOPT_SSL_VERIFYPEER => false` для работы с сайтами, у которых есть проблемы с SSL-сертификатами.
* Для парсинга HTML используется `DOMDocument` с подавлением ошибок парсинга невалидного HTML5 кода.

## 📄 Лицензия

Этот проект распространяется под лицензией MIT. Вы можете свободно использовать, модифицировать и распространять его.

---

*Разработано для SEO-специалистов и вебмастеров. v2.0 Pro.*
